/**
 * Stub reservations for forward-fill behavior inside simulate.ts (D-14).
 * See: 05-RESEARCH.md §Validation Architecture.
 */
import { describe, it } from 'vitest'

describe('forward-fill (D-14)', () => {
  it.todo('carries last close across single gap')
  it.todo('carries last close across multi-day gap')
  it.todo('increments warning count per filled day')
})
