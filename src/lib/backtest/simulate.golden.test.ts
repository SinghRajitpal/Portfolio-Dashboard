/**
 * Stub reservation for the simulate.ts golden-fixture test (Plan 05-02).
 *
 * Fixture: tests/fixtures/backtest/golden-portfolio.json — synthetic
 * 3-instrument portfolio with hand-computed expected equity curve.
 * Comparison precision: toBeCloseTo(_, 6).
 *
 * See: 05-RESEARCH.md §Validation Architecture.
 */
import { describe, it } from 'vitest'

describe('simulate golden fixture', () => {
  it.todo('synthetic 3-instrument portfolio matches expected curve toBeCloseTo(_, 6)')
})
