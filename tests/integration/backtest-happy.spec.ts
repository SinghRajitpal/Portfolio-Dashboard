import { test } from '@playwright/test'

/**
 * BACK-01: select portfolio + range, run, render curve.
 * Stub reservation — Plan 05-05/05-06 implement the page wiring.
 */
test.skip('BACK-01: select portfolio + range, run, render curve', () => {
  // Implemented by Plan 05-06 (UI shell + worker glue)
})
