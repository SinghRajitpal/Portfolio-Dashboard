import { test } from '@playwright/test'

test.describe('PORT-01: edit portfolio', () => {
  test.skip('builder loads existing portfolio with all instruments and weights pre-filled', async () => {})
  test.skip('user can change weights and reach a valid sum=100', async () => {})
  test.skip('Save persists the diff (added / removed / weight-changed rows applied)', async () => {})
})
