import { test } from '@playwright/test'

test.describe('PORT-07: template pre-fill', () => {
  test.skip('Templates dialog opens and lists Classic 60/40, All-World, All-Weather', async () => {})
  test.skip("clicking a template opens the builder pre-filled with copy + name suffix '(copy)'", async () => {})
  test.skip('saving the pre-filled portfolio persists it as a user-owned portfolio (is_template=false)', async () => {})
})
