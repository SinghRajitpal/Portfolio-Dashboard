import { test } from '@playwright/test'

test.describe('PORT-08: CSV import', () => {
  test.skip('uploading a well-formed CSV renders a preview table', async () => {})
  test.skip('unresolved tickers are flagged in the preview with an inline action to resolve', async () => {})
  test.skip('user can edit weights inline in the preview before saving', async () => {})
  test.skip('Save persists the imported rows end-to-end as a new portfolio', async () => {})
})
