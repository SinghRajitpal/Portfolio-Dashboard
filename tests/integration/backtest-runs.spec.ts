import { test } from '@playwright/test'

/**
 * D-08: identical-input run dedupes on inputs_hash.
 * Stub reservation — Plan 05-05 implements /api/backtest/runs.
 */
test.skip('D-08: identical-input run dedupes on inputs_hash', () => {
  // Implemented by Plan 05-05
})
