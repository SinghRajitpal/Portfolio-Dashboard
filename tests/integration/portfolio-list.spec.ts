import { test } from '@playwright/test'

test.describe('PORT-01: list portfolios', () => {
  test.skip('empty state renders when user has no portfolios', async () => {})
  test.skip('list renders one row per user-owned portfolio', async () => {})
  test.skip('template rows (is_template=true) are excluded from the user list view', async () => {})
  test.skip('clicking a row navigates to the builder in edit mode', async () => {})
})
