import { test } from '@playwright/test'

test.describe('PORT-01: delete portfolio', () => {
  test.skip('Delete action opens an alert-dialog confirmation', async () => {})
  test.skip('confirmed delete removes portfolio from list view', async () => {})
  test.skip('FK cascade: portfolio_instruments rows for the deleted portfolio are gone', async () => {})
})
