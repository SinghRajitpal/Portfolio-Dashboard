---
phase: 05-backtesting-engine
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/backtest/date-grid.ts
  - src/lib/backtest/forward-fill.ts
  - src/lib/backtest/inputs-hash.ts
  - src/lib/backtest/metrics.ts
  - src/lib/backtest/simulate.ts
  - src/lib/backtest/date-grid.test.ts
  - src/lib/backtest/forward-fill.test.ts
  - src/lib/backtest/inputs-hash.test.ts
  - src/lib/backtest/metrics.test.ts
  - src/lib/backtest/simulate.test.ts
  - src/lib/backtest/simulate.golden.test.ts
autonomous: true
requirements: [BACK-02, BACK-03, BACK-04, BACK-06]
tags: [pure-libs, simulation, metrics, tdd]

must_haves:
  truths:
    - "simulate() converts USD prices to CHF using point-in-time fx_rates lookups (not a single spot rate)"
    - "simulate() with drip=true and drip=false produces materially different equity curves for a portfolio with dividends"
    - "simulate() rebalances on the first trading day on/after the calendar boundary (Jan 1 / Apr 1 / Jul 1 / Oct 1)"
    - "simulate() forward-fills missing prices with last-close and reports per-instrument fill counts in warnings"
    - "computeMetrics() returns the 5 metrics: totalReturn, cagr, maxDrawdown (with peak/trough dates), sharpe (using SNB rate), vol"
    - "hashInputs() returns the same SHA-256 for two payloads that differ only in key order"
    - "D-13 — the simulation date grid is the union of all instruments' trading days (built via buildUnionDateGrid over prices + fxRates, sorted lexicographically, deduplicated, filtered to [start, end])"
    - "D-14 — missing-data policy is forward-fill last close, and the per-instrument forward-fill count is surfaced as a BacktestWarning so the UI footer can display it"
    - "D-15 — DRIP reinvests dividends on the ex-date into the paying instrument at adjusted_close on that date (when DRIP off the dividend is dropped per CONTEXT cash-sleeve=zero v1)"
    - "D-16 — rebalance fires on the first trading day on/after the calendar boundary (Jan 1 / Apr 1 / Jul 1 / Oct 1), not on the boundary itself when it falls on a weekend/holiday"
    - "D-18 — Total Return is (end-start)/start and CAGR uses exact-day basis (365.25/days) rather than a fixed-business-day approximation"
    - "D-19 — Sharpe uses the SNB CHF policy rate (stitched LZ + libor_mid per Plan 03) as the risk-free rate and annualizes by √252"
    - "D-20 — Volatility = stdev(daily returns) × √252 on a trading-days basis"
    - "D-21 — Max Drawdown = min((value − running_max) / running_max); peak and trough dates are tracked and returned in BacktestMetrics for the UI tooltip"
    - "D-23 — the benchmark is just another instrument in the engine: simulate() treats benchmark_ticker as a parallel 1-instrument sub-portfolio when provided, returning benchmarkEquity alongside the main equity curve"
  artifacts:
    - path: "src/lib/backtest/date-grid.ts"
      provides: "buildUnionDateGrid(instruments) → string[] sorted ISO dates"
      exports: ["buildUnionDateGrid"]
    - path: "src/lib/backtest/forward-fill.ts"
      provides: "forwardFillSeries(grid, prices) → { values: Map<date, number>, fillCount: number }"
      exports: ["forwardFillSeries"]
    - path: "src/lib/backtest/simulate.ts"
      provides: "Pure simulation: DRIP, rebalance, forward-fill, FX, daily step"
      exports: ["simulate"]
    - path: "src/lib/backtest/metrics.ts"
      provides: "computeMetrics(equity, snbRates, startDate, endDate) → BacktestMetrics"
      exports: ["computeMetrics"]
    - path: "src/lib/backtest/inputs-hash.ts"
      provides: "Async deterministic SHA-256 of canonical-JSON input"
      exports: ["hashInputs", "canonicalize"]
  key_links:
    - from: "src/lib/backtest/simulate.ts"
      to: "src/lib/backtest/forward-fill.ts"
      via: "import { forwardFillSeries }"
      pattern: "forwardFillSeries"
    - from: "src/lib/backtest/simulate.ts"
      to: "src/lib/backtest/date-grid.ts"
      via: "import { buildUnionDateGrid }"
      pattern: "buildUnionDateGrid"
    - from: "src/lib/backtest/metrics.ts"
      to: "SNB rates passed in BacktestInput.snbRates"
      via: "snbDailyRate = (1 + snbAnnualRate)^(1/252) - 1"
      pattern: "snb"
---

<objective>
Implement the deterministic, pure-function core of the backtest engine: date-grid construction, forward-fill, simulation loop (with DRIP + rebalance + point-in-time FX), 5-stat metrics (with SNB-based Sharpe), and the inputs-hash function. Drive every module with the test stubs already reserved in Plan 01.

Purpose: This is the math the Web Worker runs. It is Web Worker-safe (no DOM, no Supabase, no Next.js imports) and Web Worker-determinable (same inputs → same outputs, hash-verified). Every BACK-* requirement that is not pure UI plumbing terminates here.

Output: Five new TypeScript modules under `src/lib/backtest/`, each backed by its converted-from-todo vitest suite plus a golden-master integration test using the Plan 01 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-backtesting-engine/05-CONTEXT.md
@.planning/phases/05-backtesting-engine/05-RESEARCH.md
@.planning/phases/05-backtesting-engine/05-PATTERNS.md
@.planning/phases/05-backtesting-engine/05-01-SUMMARY.md
@src/lib/backtest/types.ts
@src/lib/backtest/errors.ts
@src/lib/portfolio/compute-metrics.ts
@src/lib/portfolio/normalize-weights.ts
@src/lib/data/cache-prices.ts
@tests/fixtures/backtest/golden-portfolio.json

<interfaces>
From `src/lib/backtest/types.ts` (Plan 01):
- `BacktestInput`, `BacktestOutput`, `BacktestParams`, `EquityPoint`, `AnnualBar`, `BacktestMetrics`, `BacktestWarning`, `InstrumentInput`, `PriceRow`, `DividendRow`, `FxRateRow`, `SnbRateRow`

From `src/lib/backtest/errors.ts` (Plan 01):
- `BacktestError`, kinds: `no_overlap`, `insufficient_history`, `benchmark_unavailable`, `data_gap`, `unknown`

Function signatures this plan MUST export (downstream worker + tests depend on these exact names):

```typescript
// src/lib/backtest/date-grid.ts
export function buildUnionDateGrid(args: {
  prices: PriceRow[];
  fxRates: FxRateRow[];
  startDate: string;
  endDate: string;
}): string[]  // sorted ISO 'YYYY-MM-DD' strings

// src/lib/backtest/forward-fill.ts
export function forwardFillSeries(args: {
  grid: string[];
  rows: { date: string; value: number }[];
}): { values: Map<string, number>; fillCount: number; firstDate: string | null }

// src/lib/backtest/inputs-hash.ts
export function canonicalize(value: unknown): string
export async function hashInputs(input: unknown): Promise<string>

// src/lib/backtest/metrics.ts
export function computeMetrics(args: {
  equity: EquityPoint[];
  snbRates: SnbRateRow[];
  startDate: string;
  endDate: string;
}): BacktestMetrics

// src/lib/backtest/simulate.ts
export function simulate(input: BacktestInput): {
  equity: EquityPoint[];
  benchmarkEquity: EquityPoint[] | null;
  annualBars: AnnualBar[];
  warnings: BacktestWarning[];
} | BacktestError
```

From `src/lib/portfolio/compute-metrics.ts` (Phase 4 — same pure-fn style):
- Loop-and-accumulator pattern; Map-based lookups built outside the loop; no rounding in pure layer.

From `src/lib/data/cache-prices.ts` line 48 (ISO date sort pattern):
```typescript
const sorted = [...rows].sort((a, b) => a.date.localeCompare(b.date))
```
</interfaces>
</context>

<feature>
  <name>Deterministic backtest core (5 pure libraries with TDD)</name>
  <files>src/lib/backtest/{date-grid,forward-fill,inputs-hash,metrics,simulate}.ts + matching .test.ts files</files>
  <behavior>
    Convert stubs reserved in Plan 01 to passing tests, then implement until they pass. Each module is TDD: failing test → implementation → green. Test names locked by Plan 01 stub naming.
  </behavior>
  <implementation>
    Implementation guidance is split per-task below. Each task takes 2-3 modules to keep context budget under 30%.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: date-grid + forward-fill + inputs-hash (3 small pure modules)</name>
  <files>
    src/lib/backtest/date-grid.ts,
    src/lib/backtest/forward-fill.ts,
    src/lib/backtest/inputs-hash.ts,
    src/lib/backtest/date-grid.test.ts,
    src/lib/backtest/forward-fill.test.ts,
    src/lib/backtest/inputs-hash.test.ts
  </files>
  <read_first>
    - src/lib/backtest/types.ts
    - src/lib/data/cache-prices.ts (lines 40-80 — ISO date sort + Map lookup pattern)
    - src/lib/portfolio/normalize-weights.ts (pure-fn shape reference)
    - .planning/phases/05-backtesting-engine/05-RESEARCH.md (§Pattern 5 lines 553-573 for canonicalize + hashInputs)
    - .planning/phases/05-backtesting-engine/05-PATTERNS.md (§inputs-hash.ts + §date-grid.ts + §forward-fill)
  </read_first>
  <behavior>
    - date-grid Test 1: `buildUnionDateGrid({ prices: [{date:'2020-01-02'},{date:'2020-01-03'}], fxRates: [{date:'2020-01-04'}], startDate:'2020-01-01', endDate:'2020-01-31' })` returns `['2020-01-02','2020-01-03','2020-01-04']`
    - date-grid Test 2: Dates outside [startDate, endDate] are excluded
    - date-grid Test 3: Empty inputs return `[]`
    - date-grid Test 4: Output is sorted lexicographically (== chronologically for ISO)
    - forward-fill Test 1: `forwardFillSeries({ grid: ['2020-01-01','2020-01-02','2020-01-03'], rows: [{date:'2020-01-01', value:100}, {date:'2020-01-03', value:105}] })` returns `values=Map{'2020-01-01'→100,'2020-01-02'→100,'2020-01-03'→105}`, `fillCount=1`
    - forward-fill Test 2: Multi-day gap increments fillCount per filled day
    - forward-fill Test 3: Pre-inception dates (before first row.date) are NOT filled — they are absent from the returned Map; firstDate field captures the earliest date with data
    - inputs-hash Test 1: `canonicalize({b:1,a:2})` === `canonicalize({a:2,b:1})`
    - inputs-hash Test 2: `hashInputs({a:1,b:2})` === `hashInputs({b:2,a:1})` (key reorder same hash)
    - inputs-hash Test 3: `hashInputs({a:1})` !== `hashInputs({a:2})`
    - inputs-hash Test 4: Nested objects sort recursively (`{a:{c:1,b:2}}` canonicalizes to `{"a":{"b":2,"c":1}}`)
    - inputs-hash Test 5: Arrays preserve order (do NOT sort array elements)
  </behavior>
  <action>
    RED phase: Convert the `it.todo(...)` stubs in `date-grid.test.ts`, `forward-fill.test.ts`, `inputs-hash.test.ts` into real `it(...)` assertions matching the behaviors above. Run `npx vitest run src/lib/backtest/{date-grid,forward-fill,inputs-hash}.test.ts` and confirm all tests FAIL (because production code does not exist yet).

    GREEN phase: Implement each module.

    `date-grid.ts` — flatten `prices.map(p => p.date)` and `fxRates.map(f => f.date)` into a `Set<string>`, filter to `[startDate, endDate]` inclusive range using lexicographic compare (`d >= startDate && d <= endDate`), then `[...set].sort()`. No external dep; native string compare per RESEARCH §Don't Hand-Roll line 595.

    `forward-fill.ts` — accept the grid and the rows. Sort rows by date. Track `lastValue: number | null`. Walk grid: if row exists at grid[i] use its value (lastValue = newValue); if not and lastValue !== null, fill with lastValue and increment fillCount; if not and lastValue === null, skip (pre-inception). Return `{ values, fillCount, firstDate }` where firstDate is the earliest grid date with a real (non-filled) value or `null` if no rows match.

    `inputs-hash.ts` — implement `canonicalize` per RESEARCH Pattern 5 lines 554-565 verbatim (recursive sorted-keys JSON stringifier). Implement `hashInputs` per RESEARCH Pattern 5 lines 567-573 using `crypto.subtle.digest('SHA-256', ...)`. This module must work both in the worker (`self.crypto.subtle`) and Node test runtime (`globalThis.crypto.subtle`, available in Node 20+).

    REFACTOR: Add file-level `/** */` docs explaining the contract and which RESEARCH/CONTEXT decisions they implement (D-13, D-14, D-15, D-08).

    Run `npx vitest run src/lib/backtest/{date-grid,forward-fill,inputs-hash}.test.ts` until all tests pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/backtest/date-grid.test.ts src/lib/backtest/forward-fill.test.ts src/lib/backtest/inputs-hash.test.ts --reporter=basic 2>&amp;1 | grep -E "(passed|Test Files)" &amp;&amp; test "$(grep -c "it.todo" src/lib/backtest/date-grid.test.ts src/lib/backtest/forward-fill.test.ts src/lib/backtest/inputs-hash.test.ts)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/backtest/date-grid.ts` exports `buildUnionDateGrid` matching the signature above
    - `src/lib/backtest/forward-fill.ts` exports `forwardFillSeries` returning `{ values, fillCount, firstDate }`
    - `src/lib/backtest/inputs-hash.ts` exports `canonicalize` and async `hashInputs`
    - `npx vitest run src/lib/backtest/date-grid.test.ts` exits 0 with all tests reporting "passed"
    - `npx vitest run src/lib/backtest/forward-fill.test.ts` exits 0
    - `npx vitest run src/lib/backtest/inputs-hash.test.ts` exits 0
    - `grep -c "it.todo" src/lib/backtest/date-grid.test.ts src/lib/backtest/forward-fill.test.ts src/lib/backtest/inputs-hash.test.ts | grep -v '^#'` returns 0 (no remaining todos)
    - No imports from `next/*`, `@supabase/*`, `react`, or anything DOM-related in any of the 3 production modules (worker-safe)
  </acceptance_criteria>
  <done>3 tests green; 3 modules implemented; worker-safe imports verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: metrics + simulate + golden-master test (the heart of the engine)</name>
  <files>
    src/lib/backtest/metrics.ts,
    src/lib/backtest/simulate.ts,
    src/lib/backtest/metrics.test.ts,
    src/lib/backtest/simulate.test.ts,
    src/lib/backtest/simulate.golden.test.ts
  </files>
  <read_first>
    - src/lib/backtest/types.ts (Plan 01)
    - src/lib/backtest/errors.ts (Plan 01)
    - src/lib/backtest/date-grid.ts (Task 1 — buildUnionDateGrid signature)
    - src/lib/backtest/forward-fill.ts (Task 1 — forwardFillSeries signature)
    - src/lib/portfolio/compute-metrics.ts (full file — pure-fn pattern, Map lookups, missing-data accumulator)
    - tests/fixtures/backtest/golden-portfolio.json (Plan 01)
    - .planning/phases/05-backtesting-engine/05-RESEARCH.md (§Architecture Patterns, §Pitfalls 1-9)
    - .planning/phases/05-backtesting-engine/05-PATTERNS.md (§simulate.ts + §metrics.ts)
  </read_first>
  <behavior>
    metrics tests:
    - Total Return: `computeMetrics({equity: [{date:'2020',value:100},{date:'2021',value:120}], ...}).totalReturn` toBeCloseTo `0.20`, 6
    - CAGR: equity [100 → 200] over 1827 calendar days (≈5 years) gives `(2)^(365.25/1827) - 1` toBeCloseTo `0.1487`, 4 (5th-root of 2 minus 1)
    - MDD: equity `[100,120,90,110]` gives `maxDrawdown` toBeCloseTo `-0.25`, 6, `mddPeakDate` = date of value 120, `mddTroughDate` = date of value 90
    - Sharpe: equity that is monotonically increasing at constant daily rate equal to the SNB daily rate gives Sharpe ≈ 0 (zero excess returns)
    - Sharpe NaN-guard: stdev of excess returns === 0 returns sharpe = 0 (not NaN/Infinity)
    - Volatility: daily returns with stdev = 0.01 give `vol` toBeCloseTo `0.01 * Math.sqrt(252)`, 6

    simulate tests:
    - FX point-in-time: USD prices [100→101] over a window where USD/CHF FX changes from 0.95 to 1.05 — CHF curve diverges materially from naïve `price * spotRate`
    - DRIP on vs off: portfolio with 1 known USD dividend ($1/share) — `drip=true` increases final share count and end CHF value; `drip=false` does not (cash sleeve is zero per D-15, dividend is "dropped on the floor" when DRIP off — document this in the test name)
    - Rebalance boundary (Jan 1 falls on Saturday 2022-01-01): `rebalance='annual'` fires on `2022-01-03` (first trading day in the synthetic grid); assert via warning or via observable post-rebalance weight match
    - Rebalance cadence (quarterly): `rebalance='quarterly'` over a 12-month window (2022-01-01 → 2022-12-31) fires exactly 4 rebalance events (at first trading day on/after Jan 1, Apr 1, Jul 1, Oct 1) — assert via emitted rebalance audit warnings or by counting post-rebalance weight-snap events in the returned warnings array
    - Rebalance cadence (none): `rebalance='none'` over the same 12-month window fires zero rebalance events after the initial allocation — assert the count of rebalance audit warnings is 0
    - Forward-fill warning: synthetic gap (3 missing days for instrument X) produces a `BacktestWarning` with `kind:'forward_fill', instrument_id:X, count:3`
    - No overlap: portfolio with 2 instruments whose first_dates are disjoint from the requested range returns a `BacktestError` with `kind:'no_overlap'`

    golden test:
    - Load `tests/fixtures/backtest/golden-portfolio.json`; call `simulate(fixture.input)`; assert returned equity matches `fixture.expected.equity` with `toBeCloseTo(_, 6)` per point; assert metrics match within tolerance
  </behavior>
  <action>
    RED phase: Convert `it.todo` reservations in `metrics.test.ts`, `simulate.test.ts`, `simulate.golden.test.ts` to real assertions per Behavior above. Run vitest — confirm RED.

    GREEN — metrics.ts:
    - `computeMetrics({ equity, snbRates, startDate, endDate })`.
    - Total Return: `(end - start) / start` where start = equity[0].value, end = equity[equity.length-1].value.
    - CAGR: `(end/start)^(365.25/days) - 1` where `days = (new Date(endDate) - new Date(startDate)) / 86400000` — exact calendar days (D-18).
    - Daily returns: `dailyReturns[i] = equity[i].value / equity[i-1].value - 1` for i=1..n-1.
    - Volatility: `stdev(dailyReturns) * Math.sqrt(252)` per D-20.
    - Max Drawdown: walk equity; track `runningMax` and the date achieving it; for each point compute `dd = (value - runningMax) / runningMax`; track `min(dd)` plus peak date (the runningMax-achieving date at the time the min was hit) and trough date.
    - Sharpe: build per-day SNB lookup — for each equity date, find the snb_rate row whose date_month ≤ equity_date_month, use most recent; compute `snbDaily = (1 + snbAnnual)^(1/252) - 1`. Excess = dailyReturn - snbDaily. `sharpe = mean(excess) / stdev(excess) * sqrt(252)`. Guard divide-by-zero (return 0 if stdev = 0).
    - Document each formula inline with the D-18/D-19/D-20/D-21 reference.

    GREEN — simulate.ts:
    - Implement the full simulation per RESEARCH §Architecture Patterns + §Pitfalls.
    - Steps: (1) build per-instrument price Map using forward-fill against the union date grid; (2) build per-FX-pair rate Map with ≤4-day lookback per Pitfall 5; (3) build per-instrument dividend ex-date Map; (4) compute per-instrument first-trade-date constraint and truncate startDate if needed (Pitfall 3) — emit `truncated_start` warning; if truncation leaves no overlap, return `{ kind: 'no_overlap', message }`; (5) initialize shares per instrument from `input.investmentAmount` × weight_i / 100 ÷ chf_price_at_start; (6) for each day in grid: (a) if rebalance boundary AND day == first trading day on/after boundary (D-16 — first trading day on/after the calendar boundary): recompute target shares from current CHF value; (b) if DRIP enabled AND any instrument paid a dividend ex-date in (prev_day, today]: convert dividend to CHF using `fx[ex_date, currency→CHF]`, buy fractional shares at `adjusted_close[ex_date]` for that instrument (emit `drip_on_filled` warning if ex_date was forward-filled); (c) compute CHF value: `Σ shares_i × adjusted_close[today]_i × fx[today, currency_i→CHF]`; push to equity. (7) For the benchmark, run a parallel 1-instrument sub-portfolio per D-23.
    - Build `annualBars` by computing per-calendar-year `(end_of_year_equity / start_of_year_equity) - 1` for portfolio and benchmark.
    - Use `number` math throughout. No `decimal.js` per RESEARCH §Don't Hand-Roll.

    golden-master test: read `tests/fixtures/backtest/golden-portfolio.json`, run simulate, assert each equity point's `value` matches expected with `toBeCloseTo(_, 6)`. If the fixture has a `metrics` block, also verify all 5 metrics with same tolerance.

    REFACTOR: Add file-level docs referencing the locked decisions (D-13/14/15/16/17 in simulate, D-18/19/20/21 in metrics).
  </action>
  <verify>
    <automated>npx vitest run src/lib/backtest/metrics.test.ts src/lib/backtest/simulate.test.ts src/lib/backtest/simulate.golden.test.ts --reporter=basic 2>&amp;1 | grep -E "Tests.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/backtest/metrics.ts` exports `computeMetrics` returning `BacktestMetrics` (all 5 metrics + 2 MDD dates)
    - `src/lib/backtest/simulate.ts` exports `simulate(input: BacktestInput)` returning either `{ equity, benchmarkEquity, annualBars, warnings }` or `BacktestError`
    - `npx vitest run src/lib/backtest/metrics.test.ts` exits 0, all metric tests passed
    - `npx vitest run src/lib/backtest/simulate.test.ts` exits 0, all 7 simulate tests passed (FX point-in-time, DRIP on vs off, rebalance boundary annual Jan-1-Saturday, rebalance cadence quarterly = 4 events, rebalance cadence none = 0 events, forward-fill warning, no_overlap)
    - `npx vitest run src/lib/backtest/simulate.golden.test.ts` exits 0, all equity points match within tolerance 1e-6
    - `grep -c "it.todo" src/lib/backtest/metrics.test.ts src/lib/backtest/simulate.test.ts src/lib/backtest/simulate.golden.test.ts | grep -v '^#'` returns 0
    - No imports from `next/*`, `@supabase/*`, `react`, DOM in metrics.ts or simulate.ts
    - `grep -E "decimal\\.js|date-fns|dayjs|simple-statistics" src/lib/backtest/*.ts` returns nothing (no banned deps)
  </acceptance_criteria>
  <done>All 3 vitest files green; golden-master matches fixture; worker-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BacktestInput → simulate() | Untrusted JSON payload from the API enters pure-fn boundary; bad data must not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02-DOS | Denial of service | `simulate()` with huge date range | mitigate | Date range validation happens in API route (Plan 04); pure lib trusts the caller. Plan 04 caps `end - start <= 25 years` per RESEARCH §Security Domain |
| T-5-02-NAN | Tampering (silent) | `computeMetrics` divide-by-zero | mitigate | Sharpe explicitly guards stdev=0 → returns 0; volatility on 1-point curve returns 0 (no division); CAGR guards `start <= 0` returning `NaN` with a comment that callers must validate |
| T-5-02-HASH | Tampering | `inputs-hash` collision | accept | SHA-256 cryptographic; collision probability negligible at human scale |
| T-5-02-FX | Information disclosure (mild) | FX lookback > 4 days silently uses stale rate | mitigate | Pitfall 5 — emit `fx_lookback` warning; hard-fail (BacktestError `data_gap`) when lookback > 4 days |
</threat_model>

<verification>
- `npx vitest run src/lib/backtest/ --reporter=basic` reports 0 failures, 0 todos
- `npx tsc --noEmit` exits 0
- No banned imports (grep verification in acceptance_criteria)
- All 5 modules export the exact function signatures listed in `<interfaces>`
</verification>

<success_criteria>
- Plan 04 (API routes), Plan 05 (worker + charts), and Plan 06 (UI) can import every pure function from `src/lib/backtest/*` and trust the contract
- The engine satisfies BACK-02 (point-in-time FX), BACK-03 (DRIP toggle), BACK-04 (rebalance), BACK-06 (5 metrics with SNB Sharpe) at the pure-library layer
</success_criteria>

<output>
Create `.planning/phases/05-backtesting-engine/05-02-SUMMARY.md` when done. Include: modules created, test counts, any deviations from RESEARCH patterns (and why), golden-master tolerance achieved.
</output>
