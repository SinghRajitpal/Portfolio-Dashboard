---
phase: 05-backtesting-engine
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - tests/integration/backtest-happy.spec.ts
  - tests/integration/backtest-chart.spec.ts
  - tests/integration/backtest-benchmark.spec.ts
  - tests/integration/backtest-annual-bars.spec.ts
  - tests/integration/backtest-runs.spec.ts
  - tests/integration/backtest-stale.spec.ts
autonomous: false
requirements: [BACK-01, BACK-05, BACK-07, BACK-08]
tags: [integration-tests, playwright, e2e, manual-verify]

must_haves:
  truths:
    - "Playwright spec for BACK-01 (happy path) passes: select seeded portfolio + date range + Run → equity curve canvas + metrics strip rendered with 5 values"
    - "Playwright spec for BACK-05 (equity chart) passes: chart canvas visible, crosshair hover shows tooltip with CHF value"
    - "Playwright spec for BACK-07 (benchmark) passes: selecting a benchmark adds an overlay series; deselecting removes it"
    - "Playwright spec for BACK-08 (annual bars) passes: annual bars canvas visible after running a backtest with span > 1 year"
    - "Playwright spec for D-08 (run dedup) passes: clicking Run twice with identical params writes only one row (deduped:true on the second response)"
    - "Playwright spec for D-09 (stale-on-view) passes: load a run, simulate prices update (via cron route or direct DB write), reload run via drawer — stale badge appears"
    - "User manually confirms the full UX feels responsive (D-02 auto-rerun on cheap toggles) and the run summary footer reads accurately"
  artifacts:
    - path: "tests/integration/backtest-happy.spec.ts"
      provides: "End-to-end BACK-01 happy path"
    - path: "tests/integration/backtest-chart.spec.ts"
      provides: "BACK-05 equity chart + crosshair"
    - path: "tests/integration/backtest-benchmark.spec.ts"
      provides: "BACK-07 benchmark overlay toggle"
    - path: "tests/integration/backtest-annual-bars.spec.ts"
      provides: "BACK-08 annual bars rendering"
    - path: "tests/integration/backtest-runs.spec.ts"
      provides: "D-08 run dedup via inputs_hash"
    - path: "tests/integration/backtest-stale.spec.ts"
      provides: "D-09 stale-on-view badge"
  key_links:
    - from: "tests/integration/backtest-*.spec.ts"
      to: "/dashboard/backtest UI (Plan 06)"
      via: "Playwright chromium page.goto"
      pattern: "/dashboard/backtest"
---

<objective>
Convert the 6 Playwright stub specs reserved in Plan 01 into passing integration tests against the live Plan 06 UI. End with a manual UX checkpoint that confirms the responsive feel of the workbench (D-02 auto-rerun on cheap params, deterministic outputs, run summary footer accuracy).

Purpose: Plan 02 covered pure-lib BACK-02/03/04/06 via vitest. This plan covers the user-observable BACK-01/05/07/08 paths via Playwright + a human eyes-on check. The phase cannot pass /gsd:verify-work without both.

Output: 6 passing Playwright specs + a manual checkpoint sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-backtesting-engine/05-CONTEXT.md
@.planning/phases/05-backtesting-engine/05-06-SUMMARY.md
@tests/integration/backtest-happy.spec.ts
@tests/integration/backtest-chart.spec.ts
@tests/integration/backtest-benchmark.spec.ts
@tests/integration/backtest-annual-bars.spec.ts
@tests/integration/backtest-runs.spec.ts
@tests/integration/backtest-stale.spec.ts
@playwright.config.ts
@tests/integration/portfolios-list.spec.ts

<interfaces>
All 6 spec files exist as Plan 01 stubs (test.skip) with reserved titles. Convert each test.skip(...) to test(...) with real assertions.

Test prerequisites (set up in each spec or in a shared fixture):
- Signed-in user (auth helper used by existing integration specs; reuse the same fixture)
- At least one saved portfolio with 2+ instruments (seed via the existing test setup or by hitting POST /api/portfolios)
- Phase 3 prices, dividends, fx_rates already seeded for those instruments (production seed handles this)
- snb_rates seeded (Plan 03 seed-snb script run once before tests; can be a beforeAll hook that runs npm run seed:snb or hits POST /api/cron/refresh-snb with valid CRON_SECRET)

DOM/canvas selectors (the executor finalizes; suggestions below):
- Setup bar: [data-testid="backtest-setup-bar"]
- Portfolio Select: [data-testid="backtest-portfolio-select"]
- Start input: input[type="date"][name="start"]
- End input: input[type="date"][name="end"]
- DRIP toggle: input[type="checkbox"][name="drip"] OR Switch role
- Rebalance tabs: [role="tab"][data-state="active"] etc.
- Benchmark Select: [data-testid="backtest-benchmark-select"]
- Run button: button:has-text("Run")
- Equity chart canvas: [data-testid="equity-curve-chart"] canvas
- Metrics strip: [data-testid="metrics-strip"] with 5 stat children
- Annual bars canvas: [data-testid="annual-returns-chart"] canvas
- Footer: [data-testid="run-summary-footer"]
- History button: button:has-text("History")
- Stale badge: text=Data refreshed
- Recompute button: button:has-text("Recompute")

The executor must add the `data-testid` attributes to the Plan 06 components if not present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert all 6 Playwright stubs to passing specs + add data-testid attributes</name>
  <files>
    tests/integration/backtest-happy.spec.ts,
    tests/integration/backtest-chart.spec.ts,
    tests/integration/backtest-benchmark.spec.ts,
    tests/integration/backtest-annual-bars.spec.ts,
    tests/integration/backtest-runs.spec.ts,
    tests/integration/backtest-stale.spec.ts
  </files>
  <read_first>
    - tests/integration/portfolios-list.spec.ts (existing spec — auth fixture + setup pattern)
    - tests/integration/portfolios-builder.spec.ts (existing spec — interaction pattern)
    - playwright.config.ts (project config + base URL + storage state)
    - src/app/dashboard/backtest/BacktestClient.tsx (Plan 06 — identify selectors)
    - src/components/backtest/BacktestSetupBar.tsx (Plan 06)
    - src/components/backtest/EquityCurveChart.tsx (Plan 05 — canvas element shape)
    - src/components/backtest/MetricsStrip.tsx (Plan 06)
    - src/components/backtest/RunHistoryDrawer.tsx (Plan 06)
  </read_first>
  <action>
    Survey Plan 06 components and add stable data-testid attributes where the specs depend on them: backtest-setup-bar (root of setup bar), backtest-portfolio-select, backtest-benchmark-select, equity-curve-chart, annual-returns-chart, metrics-strip, run-summary-footer, run-history-drawer, stale-badge. Add via the existing component files in src/components/backtest/* and src/app/dashboard/backtest/BacktestClient.tsx — small edits, one-line each.

    Convert each test.skip in the 6 spec files to test() with the assertions below. Each spec begins with signed-in fixture + seeded-portfolio + seeded-snb-rates setup (mirror the auth pattern from portfolios-list.spec.ts).

    backtest-happy.spec.ts — BACK-01 happy path:
    1. page.goto('/dashboard/backtest')
    2. expect [data-testid="backtest-setup-bar"] visible
    3. Pick the first portfolio in the Select; pick start = 5 years ago; end = today; leave DRIP off; leave rebalance = annual; leave benchmark = None.
    4. Click Run; await network idle on /api/backtest/data and /api/backtest/runs.
    5. expect [data-testid="equity-curve-chart"] canvas visible
    6. expect [data-testid="metrics-strip"] has 5 visible stat children (use locator.count() === 5)
    7. expect [data-testid="run-summary-footer"] contains text "Idealized" (D-17 disclosure)

    backtest-chart.spec.ts — BACK-05 equity chart + crosshair:
    1. Same setup as happy path; run a backtest.
    2. Hover over the equity chart canvas at a center pixel position (page.mouse.move).
    3. expect a tooltip element appears with text matching /CHF\s+\d/ (Swiss CHF format).
    4. expect the tooltip contains a date string matching /\d{4}-\d{2}-\d{2}/.

    backtest-benchmark.spec.ts — BACK-07 benchmark overlay:
    1. Same setup; run a backtest with benchmark = None first.
    2. Take a screenshot of the equity chart (locator screenshot).
    3. Change benchmark Select to 'S&P 500 (SPY.US)' or similar; await worker re-run (no /api/backtest/data fetch — verify via page.on('request') tracking that only /api/backtest/runs POST fires).
    4. Take a second screenshot; assert it differs from the first (chart now has 2 series).
    5. Change benchmark back to 'None'; verify the overlay disappears.

    backtest-annual-bars.spec.ts — BACK-08 annual bars:
    1. Run a backtest spanning > 1 year.
    2. expect [data-testid="annual-returns-chart"] canvas visible.
    3. Switch benchmark to a real one; expect a second chart canvas appears (per RESEARCH Pattern 3 recommendation (a) — two stacked charts).

    backtest-runs.spec.ts — D-08 dedup:
    1. Run a backtest with specific params.
    2. Capture network response for POST /api/backtest/runs — expect { id: ..., deduped: false } (first run).
    3. Click Run again without changing params; await /api/backtest/runs POST.
    4. Capture response — expect { id: same, deduped: true }.

    backtest-stale.spec.ts — D-09 stale-on-view:
    1. Run a backtest; capture the run id.
    2. Simulate a prices refresh: either (a) hit POST /api/cron/refresh-prices with CRON_SECRET (from test env) and exchange=US, OR (b) directly UPDATE one row in the prices table for an instrument in the portfolio via Supabase admin client to bump updated_at.
    3. Open History drawer; click the captured run id.
    4. expect [data-testid="stale-badge"] visible.
    5. expect a 'Recompute' button is rendered.
    6. Click Recompute; expect a new /api/backtest/data fetch AND a new /api/backtest/runs POST.

    For tests that require beforeAll database setup (seeding snb_rates), add a single beforeAll hook in a shared file or per-spec that hits POST /api/cron/refresh-snb with CRON_SECRET (process.env.CRON_SECRET; must be set in playwright.config.ts env or .env.test). If CRON_SECRET is not set, document the prerequisite in the spec file's top comment and have the test soft-skip with test.skip(!process.env.CRON_SECRET, 'CRON_SECRET required').
  </action>
  <verify>
    <automated>grep -c "test.skip" tests/integration/backtest-happy.spec.ts tests/integration/backtest-chart.spec.ts tests/integration/backtest-benchmark.spec.ts tests/integration/backtest-annual-bars.spec.ts tests/integration/backtest-runs.spec.ts tests/integration/backtest-stale.spec.ts | grep -v ':0$' | grep -v '^#' &amp;&amp; echo "FAIL: skips remain" &amp;&amp; exit 1 ; npx playwright test tests/integration/backtest-*.spec.ts --project=chromium --reporter=line 2>&amp;1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 spec files have 0 test.skip calls (real test() calls now)
    - npx playwright test tests/integration/backtest-*.spec.ts --project=chromium exits 0
    - Each spec asserts at least one DOM/canvas observation specific to its BACK-* or D-* requirement
    - data-testid attributes added to Plan 06 components: backtest-setup-bar, backtest-portfolio-select, backtest-benchmark-select, equity-curve-chart, annual-returns-chart, metrics-strip, run-summary-footer, run-history-drawer, stale-badge (grep -c "data-testid" src/components/backtest/*.tsx src/app/dashboard/backtest/*.tsx ≥ 9)
    - backtest-benchmark.spec.ts verifies via request tracking that toggling benchmark does NOT trigger /api/backtest/data fetch (only /api/backtest/runs POST)
    - backtest-runs.spec.ts asserts deduped:false on first run and deduped:true on second
  </acceptance_criteria>
  <done>All 6 specs green in chromium; data-testid coverage complete; full integration suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UX checkpoint — full Phase 5 walkthrough</name>
  <read_first>
    - .planning/phases/05-backtesting-engine/05-CONTEXT.md (D-01 through D-09 — UX decisions)
    - .planning/phases/05-backtesting-engine/05-06-SUMMARY.md
  </read_first>
  <files>n/a (verification-only checkpoint; Task 1 wrote the spec files and Plan 06 wrote the UI components)</files>
  <action>Sign in to the app, navigate to /dashboard/backtest, and walk through the 11 numbered observations in `<how-to-verify>`. Document any failures. Halt until "approved" is typed.</action>
  <verify><human-check>User confirms all 11 observations match expected behavior; explicitly types "approved"</human-check></verify>
  <what-built>
    The full /dashboard/backtest experience: setup bar, equity curve with crosshair, metrics strip, annual bars, run summary footer with warnings, run history drawer, stale-on-view badge.

    Automated tests cover individual behaviors but cannot judge the "precision instrument" feel CONTEXT calls for — toggling DRIP feels like a calculator (no spinner-induced lag perception); the metrics strip reads correctly at a glance; the warning footer is honest about what was forward-filled.

    This checkpoint asks the user to perform the workbench flow once and confirm it meets the bar.
  </what-built>
  <how-to-verify>
    1. Sign in to the app (live or local) and navigate to /dashboard/backtest.
    2. Select a seeded portfolio (e.g., a Classic 60/40 template or any portfolio with 2+ instruments).
    3. Set start date to 5 years ago; end date to today.
    4. Click Run. Observe:
       - Equity curve renders within ~2 seconds of click
       - Metrics strip shows 5 values (Total Return %, CAGR %, Max Drawdown %, Sharpe, Volatility %)
       - Annual bars render
       - Run summary footer lists parameters + the 'Idealized: no fees, no taxes' disclosure
    5. Toggle DRIP on. Observe:
       - Equity curve re-renders within ~500ms (no /api/backtest/data fetch — verify Network tab)
       - Metrics strip values change (total return increases)
    6. Change rebalance from 'annual' to 'quarterly'. Observe: equity curve and metrics update without a data fetch.
    7. Select a benchmark (e.g., S&P 500). Observe:
       - A second line appears on the equity chart
       - A second histogram appears in the annual bars
       - Crosshair tooltip shows both portfolio and benchmark values
    8. Change the date range (start moves earlier). Observe:
       - A network fetch to /api/backtest/data fires (verify Network tab)
       - The earliest-allowed-start helper text appears under the start input if applicable ('constrained by SPY listing 2010-04-12' or similar)
    9. Open the History drawer. Observe:
       - All your recent runs are listed, most recent first
       - Each row shows date range + a key metric + relative timestamp ('5 minutes ago')
       - The currently-loaded run is highlighted
    10. Click a prior run. Observe: setup bar parameters are restored; results render from cached data (no /api/backtest/data fetch).
    11. (Optional) Trigger a prices refresh (run `npm run dev` then curl POST /api/cron/refresh-prices with CRON_SECRET, or wait for the daily cron). Reload the same prior run. Observe: 'Data refreshed — recompute?' badge appears; clicking Recompute re-runs the backtest.

    Reject the checkpoint if:
    - Any of the 11 observations fails
    - The UX feels "form submission" rather than "calculator" — i.e., perceptible lag on cheap-param toggles
    - The footer omits the 'Idealized' disclosure
    - The crosshair tooltip is broken or shows non-CHF formatting
    - The stale badge fails to appear after a real prices refresh
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues observed.</resume-signal>
  <acceptance_criteria>
    - All 11 numbered observations confirmed by the user
    - User explicitly types "approved"
    - If rejected, the issues are itemized so a follow-up gap-closure plan can address them
  </acceptance_criteria>
  <done>User signs off on the Phase 5 UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → live API + DB | Tests reuse the same RLS-scoped auth fixtures as prior phases; no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07-FLAKE | Tampering (test reliability) | Canvas-based chart visual assertions | mitigate | Tests assert canvas-element presence + tooltip text content, NOT visual pixel diffs; chromium-only project (no cross-browser flake); explicit page.waitForResponse for API calls before assertions |
| T-5-07-CRON-PRIV | Spoofing | Tests calling /api/cron/refresh-* with CRON_SECRET | mitigate | Test env CRON_SECRET separate from prod CRON_SECRET; documented in spec top comments; soft-skip when missing |
| T-5-07-DATA-MUTATE | Tampering | Stale spec writes to prices.updated_at | accept | Test uses a dedicated seeded test user's data only; isolated by RLS; no impact on real data |
</threat_model>

<verification>
- npx playwright test tests/integration/backtest-*.spec.ts --project=chromium exits 0
- npm run test exits 0 (unit + integration combined)
- npm run build exits 0
- Manual checkpoint user-approved
</verification>

<success_criteria>
- All 8 BACK-* requirements have at least one automated verification (Plan 02 unit tests cover BACK-02/03/04/06; Plan 07 integration tests cover BACK-01/05/07/08)
- All 9 CONTEXT D-* decisions have observable evidence (5 in unit tests, 4 in integration, all in manual checkpoint)
- Phase 5 is ready for /gsd:verify-work
</success_criteria>

<output>
Create .planning/phases/05-backtesting-engine/05-07-SUMMARY.md when done. Include: spec file count + test count per file + total runtime, manual checkpoint result, any UX issues surfaced + how addressed, recommendation for whether to proceed to /gsd:verify-work or run a gap-closure plan first.
</output>
